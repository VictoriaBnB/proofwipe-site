{
  "name": "proofwipe-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Marketing website for ProofWipe: verified disk sanitization.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "postbuild": "pagefind --site dist && node scripts/check-guards.mjs",
    "guards": "node scripts/check-guards.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^7.0.6",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.5.2",
    "wrangler": "^4.107.0"
  }
}
